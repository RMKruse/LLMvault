# Third-party notices

The LLMvault release assets contain no bundled third-party runtime code. Obsidian provides the host API and is not included. Development-only packages and their licenses are recorded in `package-lock.json` and are not shipped in `main.js`, `manifest.json`, or `styles.css`.
